{
  "name": "safesub-ai-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Thin AI-advisor proxy for safesub: holds the API key, enforces the no-doses refusal layer in code, remembers nothing.",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

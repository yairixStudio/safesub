# safesub — מסמך ההתמצאות

> נקודת הכניסה לפרויקט. אם אתה סוכן AI שנכנס לכאן — **קרא את הקובץ הזה במלואו לפני שאתה נוגע בקוד.**
> עודכן לאחרונה: **2026-08-28**

---

## מה זה safesub

אפליקציית מובייל בעברית ל**צמצום נזקים** (harm reduction). לא לעידוד שימוש, ולא לגמילה — **ליווי מפוכח של אנשים שכבר עושים בחירות.**

הרעיון: האויב אינו החומר אלא חוסר המידע והתזמון. לכן במקום יומן שמתעד בדיעבד, safesub מציגה **בזמן אמת מה פעיל בגוף כרגע** — עקומת עלייה/שיא/דעיכה מבוססת נתונים פרמקוקינטיים מהספרות, עם קו "עכשיו", ומנוע שמזהה **חפיפה מסוכנת בין חומרים פעילים** ומדרג אותה (מסוכן / לא בטוח / זהירות).

**קהל יעד:** מבוגרים 18+ שצורכים באופן לא-בעייתי או אקספרימנטלי. הקשר ישראלי מובנה: עברית RTL, משאבי חירום מקומיים, והבהרה שאין בישראל שירות בדיקת חומרים חוקי.

---

## ארבעת החוקים הקשיחים

אלה לא העדפות עיצוב. הם גם זהות המוצר וגם עמדת הציות מול חנויות האפליקציות. **אל תפר אותם, ואל תציע להפר אותם.**

1. **לעולם לא מינונים.** לא כמויות מומלצות, לא לוחות מינון-חוזר, לא הוראות שימוש, לא הוראות הכנה או השגה. מותר: תזמון, חפיפות, ועקרונות בטיחות (הידרציה, לא לשלב מדכאים, לא לבד, לא לנהוג).
2. **לא ייעוץ רפואי.** אין אבחון, אין מרשם. בכל מקום רלוונטי — תזכורת לפנות לרופא, והפניה מיידית לעזרה אנושית במצבי חירום (מד״א 101, ער״ן 1201).
3. **הכל הערכה, לא מדידה.** כל מספר במוצר הוא ממוצע אוכלוסייה מהספרות. שונות בין-אישית (משקל, מין, גנטיקה, תפקוד כבד) מוצגת בגלוי, וכך גם היכן הנתונים חזקים והיכן חלשים.
4. **פרטיות כערך-על.** הנתונים מקומיים במכשיר. אין חשבון, אין ענן. היוצא היחיד מהמכשיר הוא תקציר מצב אנונימי ליועץ ה-AI — ראה [`docs/ai-advisor.md`](docs/ai-advisor.md).

**טון:** לא מטיף, לא מפחיד, לא מבטיח "בטוח". נותן בהירות ומחזיר את ההחלטה למשתמש.

---

## מצב הקוד — קרא את זה לפני שאתה עורך

**`demo-reference.html`** הוא **דמו-ייחוס, לא האפליקציה.** הוא מקור האמת ל:

| מה | איפה בקובץ |
|---|---|
| שפת העיצוב, ה-CSS, RTL/LTR, טוקני ערכת-נושא (כהה/בהיר) | `<style>` בראש — `:root` / `html[data-theme=light]` |
| **קטלוג** החומרים (`tp`, `hl`, `kind`, `ev`) — ניטרלי לשפה, מאחורי החיפוש | `const SUBS` (~68 רשומות) |
| **רשימת המשתמש** — מה שמופיע בגריד. ריקה בהפעלה ראשונה, בכוונה | `let MY` |
| כל המחרוזות בשתי שפות, כולל שמות/הערות/טיפים/כינויי-חיפוש לחומרים | `const L` → `L.he` / `L.en`, גישה רק דרך `t()` |
| מנוע ה-PK | `prep()` / `levelAt` — ראה אזהרה למטה |
| מנוע האינטראקציות | `RISK` (זוגות) → `IDCAT` (חומר×קטגוריה) → `CATRISK` (קטגוריה×קטגוריה) / `riskFor` / `MED_FLAGS` |
| רינדור הגרף | `renderChartSvg` |
| אריחי הגריד: נקודת פעילות, ספירה בחלון, ספארקליין | `tileHTML` / `renderGrid` / `sparkFor` / `closeAt` |
| עמודים מלאים (הגדרות, פרופיל משתמש, למידה) | `.page` — `renderSettings` / `openProfilePage` / `openProfile` |
| שמירה מקומית — **דמו בלבד** | `persist()` / `restore()` / `wipeAll()` על `localStorage` |
| משאבי חירום ישראליים | `const RESOURCES` (טקסטים ב-`L.*.res`) |

**עבר ריפקטור ביצועים ב-2026-08-26.** המתמטיקה של ה-PK לא השתנתה בכלל (מאומת bit-identical), אבל המבנה כן: `effShape`/`doseIntensity`/`entriesBy` התמזגו ל-`prep()`, שמשטח כל רישום פעם אחת למחזור לסקלרים שהלולאה הפנימית צריכה. `byId`/`riskFor` הם Map. `renderGrid` מטליא צמתים במקום לבנות מחדש. `refresh()` מאחד רינדורים ל-rAF אחד.

**עבר שכבת מוצר ב-2026-08-28** (גם כאן — הבלוק של המנוע bit-identical):
- **i18n.** אין מחרוזת בקוד; הכל ב-`L.he`/`L.en` (~730 מפתחות לשפה). ברירת מחדל: **עברית + כהה** (החלטת מוצר 2026-08-28, במקום זיהוי שפת המכשיר); החלפה בדרופדאון בפתיחה או בהגדרות. `sub` ברישום ו-`s` בקטלוג הם **מפתחות** (`'edible'`, `'la'`), לא תוויות — `test/fixture.mjs` עודכן בהתאם, ה-golden לא.
- **קטלוג ↔ רשימה.** `SUBS` הוא קטלוג עשיר שלא מוצג לעולם; `MY` הוא מה שהמשתמש הוסיף דרך החיפוש — ורק זה בגריד. זו עמדה מוצרית (לא חושפים חומרים), לא מגבלה טכנית.
- **ערכת נושא.** כל צבע הוא טוקן; `html[data-theme]` נכתב מההגדרות (מערכת/כהה/בהיר). אין צבעי משטח/טקסט קשיחים ב-CSS או ב-SVG שנבנה ב-JS — שמור על זה.
- **עמודים** במקום מגירות: הגדרות, פרופיל, למידה — `.page` עם כפתור חזרה.
- **תוספת לחוק 3:** לכל רשומה בקטלוג `ev:'A'|'B'|'C'` (חוזק הנתונים) שמוצג במסך הלמידה.
- **RN על אמולטור אנדרואיד — 11/11 זרימות Maestro עוברות (2026-08-28).** לפני שנוגעים ב-UI: [`docs/testing.md`](docs/testing.md) → "לקחים לדרייברי UI" (ספירה לאחור ב-SVG, תפריטים ברמת השורש, מקלדת, סימני כיווניות). הפרה של אחד מהם תפיל את הסוויטה בלי שהאפליקציה תיראה שבורה.

### ⚠️ לפני שאתה נוגע במנוע — יש בדיקה, הרץ אותה

```bash
node test/run.mjs        # PASS = העקומות לא זזו
```

היא מושכת את הקוד **מתוך** `demo-reference.html` (לפי העוגנים `const C = {nic:` … `/* category-level emergency signs */` ו-`PER-CYCLE CACHES` … `/* ================= substances grid`) ומשווה 2,316 דגימות עקומה מול `test/golden-pk.json` (המצב מלפני הריפקטור). **כל מה שבין העוגנים חייב לרוץ ב-Node בלי DOM** — אל תכניס לשם `matchMedia`, `document` או `L`/`t()` ברמה העליונה. **שום דבר בממשק לא יגיד לך שהעקומות זזו** — והעקומות הן המוצר. הרץ אחרי כל שינוי ב-`prep()`, `levelAt`, `windowLeft`, `activeIds`, `todayCount` או `SUBS`.

שינוי מכוון במודל או ב-`SUBS`? רק אז, ובמודע: `node test/run.mjs --update`.

**מה שעדיין לא קיים — אלה לא פרטי פורט, אלה עבודה אמיתית:**

- ⚠️ **שמירה — רק לדפדפן.** מאז 2026-08-28 הדמו שומר הכל (`LOG`, `MY`, פרופיל, הגדרות, חומרים מותאמים) ב-`localStorage` דרך `persist()/restore()`. זה **לא** `store.js` של stack.md: ב-native WKWebView מפנה localStorage תחת לחץ, ולכן פעולה 2 עדיין חוסמת.
- ✅ ~~קריאת ה-AI לא יכולה לעלות לאוויר~~ — יש proxy ב-`proxy/` (שכבת סירוב + מפתח בצד שרת); האפליקציה מחוברת דרך `EXPO_PUBLIC_AI_PROXY` עם נפילה חיננית ל-`localReply()`. נותרה פריסה ([docs/ai-advisor.md](docs/ai-advisor.md)). מזהה המודל בדמו עודכן.
- ✅ ~~אין build system, אין הפרדה למודולים, אין מעטפת native~~ — יש: `app/` (Expo, מודולים, Android build). iOS טרם נבדק.

---

## הסטאק

**React Native (Expo SDK 57, TypeScript) — הוחלט ב-2026-08-28**, בניגוד להמלצת stack.md המקורית (Vite+Capacitor). האפליקציה חיה ב-[`app/`](app/) והיא **הקוד**; `demo-reference.html` נשאר דמו-ייחוס לעיצוב ולמתמטיקה.

```
app/
  src/engine/     catalog.ts · rules.ts (GENERATED מהדמו) · pk.ts · interactions.ts · types.ts
  src/i18n/       he.ts · en.ts (GENERATED) · index.ts (t, makeTr)
  src/state/      store.ts (AsyncStorage) · AppContext.tsx (מנוע/מתרגם/ערכת-נושא למחזור)
  src/logic/      advisor.ts (קריאת מצב, תקציר אנונימי, מענה מקומי)
  src/ui/         Tile · SubstancesPane · Chart · IntensityPane · JournalPane · LogSheet · CtxMenu · Onboarding · pages/
  __tests__/      pk.golden.test.ts — אותו golden, אותן 2,316 דגימות, drift 0
  e2e/            Maestro flows — סימולציות משתמש על אמולטור אנדרואיד (./e2e/run.sh)
```

**כללי ברזל של הפורט:**
- `catalog.ts`, `rules.ts`, `he.ts`, `en.ts`, `icons.ts` **נוצרים** מהדמו (`scratchpad/gen-ts.mjs` בסשן; הלוגיקה: לחלץ את `SUBS`/`RISK`/`L` מ-`demo-reference.html`). שינוי תוכן → קודם בדמו, ואז מייצרים מחדש. אל תערוך אותם ביד.
- כיווניות (RTL/LTR) היא של האפליקציה, לא של Android: `I18nManager.allowRTL(false)` + `supportsRtl="false"` (plugin `plugins/withNoRtl.js`). כל שורה משתמשת ב-`dir.row`, כל טקסט ב-`dir.textAlign`. אל תשתמש ב-`start`/`end` של RN.
- `cd app && npm test` (golden) ו-`npm run typecheck` לפני כל קומיט. `npm run android:release` בונה ומתקין על האמולטור.

ההשוואה המקורית (ולמה בכל זאת RN): [`docs/stack.md`](docs/stack.md).

---

## סדר הפעולות

מה לעשות, לפי הסדר. `[חוסם]` = אי אפשר לשחרר בלי זה.

| # | פעולה | מסמך |
|---|---|---|
| 1 | ~~Vite scaffold~~ → **נעשה ב-RN** (`app/`), 2026-08-28 | [stack](docs/stack.md) |
| 2 | ~~`store.js`~~ → **נעשה**: `app/src/state/store.ts` על AsyncStorage (SQLite ב-Android) | [stack](docs/stack.md) |
| 3 | ~~proxy ל-AI + שכבת סירוב~~ → **הקוד נעשה** (`proxy/`, ‏2026-09-02, ‏40 בדיקות לשכבת הסירוב); **[חוסם]** נותרה פריסה: `wrangler secret put` + `deploy` | [ai-advisor](docs/ai-advisor.md) |
| 4 | פונטים מקומית, safe-area, אימות `tel:101`, חישוב מחדש ב-resume | [stack](docs/stack.md) |
| 5 | ~~פרסום מסמך המתודולוגיה~~ → **נעשה**: [`docs/methodology.md`](docs/methodology.md) (2026-08-31) | [app-store](docs/app-store.md) |
| 6 | מכתב גיבוי מאל-סם / חוף מבטחים | [app-store](docs/app-store.md) |
| 7 | TestFlight חיצוני — בדיקת מיצוב זולה לפני הגשה אמיתית | [app-store](docs/app-store.md) |
| 8 | הגשה: דירוג 18+, Review Notes, תיאור חנות | [app-store](docs/app-store.md) |

---

## אזהרה לסוכני AI — עובדות מתכלות

המסמכים כאן מכילים עובדות שנכונות **ל-2026-08-26** ומתיישנות. אל תבנה עליהן בלי לאמת:

- **מזהה מודל Claude ותחביר ה-API** — טען את הסקיל `claude-api` וקח משם את המזהה העדכני. **אל תסמוך על המחרוזת שכתובה במסמכים כאן.** הדמו כבר נכשל בדיוק ככה: הוא נושא `claude-sonnet-4-6` מיושן.
- **נוסח ה-App Store Review Guidelines** (1.4.1 / 1.4.2 / 1.4.3) ומדרג הגילאים — משוך מחדש מ-developer.apple.com לפני שאתה בונה חבילת הגשה.
- **תקדימים בחנות** (KnowDrugs, app id 1152858301) — ודא שהאפליקציה עדיין שם לפני שאתה מצטט אותה כטיעון.

---

## המסמכים

| קובץ | מה יש בו |
|---|---|
| [`test/run.mjs`](test/run.mjs) | בדיקת golden לעקומות ה-PK — הרץ לפני ואחרי כל נגיעה במנוע |
| [`docs/stack.md`](docs/stack.md) | החלטת הטכנולוגיה, ההשוואה ל-RN/Flutter, אחסון מקומי, צ'קליסט פורט |
| [`docs/app-store.md`](docs/app-store.md) | אסטרטגיית כניסה ל-App Store ו-Play: הסעיפים, המלכודות, חבילת ההגשה, ערעור |
| [`docs/ai-advisor.md`](docs/ai-advisor.md) | ארכיטקטורת ה-proxy, מה חוצה את הרשת, שכבת הסירוב, ה-system prompt |
| [`docs/testing.md`](docs/testing.md) | שלוש שכבות הבדיקה: golden בדמו, Jest באפליקציה, 11 זרימות Maestro על אמולטור |
| [`docs/methodology.md`](docs/methodology.md) | מקורות ה-PK ומטריצת הסיכונים, האימות מול הספרות (2026-08-31), גבולות המודל — נדרש ל-1.4.1 |
| [`docs/production.md`](docs/production.md) | מוכנות פרודקשן: אמינות, ביצועים, אנרגיה, אכיפת פרטיות, והפערים שנשארו (חתימה, proxy, iOS) |
